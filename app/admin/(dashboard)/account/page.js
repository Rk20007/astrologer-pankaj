import AccountClient from '../../_components/AccountClient';

export const dynamic = 'force-dynamic';

export const metadata = { title: 'Login & password' };

export default function AccountPage() {
  return <AccountClient />;
}
